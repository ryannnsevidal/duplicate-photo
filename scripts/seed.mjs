 /* eslint-env node */
import process from 'node:process';

async function main() {
	// Placeholder: add any seed logic here
	console.log('Seed complete (no-op)');
}

main().catch((err) => {
	console.error(err);
	process.exit(1);
});