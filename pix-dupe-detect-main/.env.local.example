# Copy to .env.local and fill in your values
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
# Optional: toggle E2E banner and admin test gating (keep 0 in prod)
VITE_E2E_TEST_MODE=0

# Optional: local-only E2E creds for tests (do not commit real secrets)
E2E_EMAIL=test@example.com
E2E_PASSWORD=Password123!